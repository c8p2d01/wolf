#include "../../inc/gifdec.h"
#include "../../inc/gif_structs.h"

int	min(int A, int B)
{
	if ((A) < (B))
		return (A);
	return (B);
}

int	max(int A, int B)
{
	if ((A) > (B))
		return (A);
	return (B);
}
